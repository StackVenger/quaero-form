pre-commit:
  commands:
    lint:
      glob: "*.{js,ts,cjs,mjs,d.cts,d.mts,jsx,tsx,json,jsonc,css,html}"
      run: git-format-staged --formatter 'biome check --write' .

commit-msg:
  commands:
    commitlint:
      run: commitlint --edit {1}
